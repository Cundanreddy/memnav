# memory_analyzer

A Rust crate for analyzing `.map` files and reporting per-symbol, per-function, and per-section memory usage.

### Usage (CLI)
```bash
memory_analyzer analyze app.map --csv report.csv --json report.json
```